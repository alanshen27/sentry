# Sentry env (all optional - app runs fully on cached demo data)
NEXT_PUBLIC_MAPBOX_TOKEN=
FIRMS_MAP_KEY=
OPENAI_API_KEY=
OPENROUTER_API_KEY=
# Optional: enables shared cache + cross-instance live presence (pub/sub).
# Without it, presence falls back to an in-process memory store + event bus.
REDIS_URL=
